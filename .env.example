# Copy this file to .env for local development, then add real values locally.
# On Render, configure the same variables in the service Environment settings.
SKILLPATH_SITE_URL=https://your-production-domain.example
SKILLPATH_API_KEY=replace-with-a-server-only-api-key
